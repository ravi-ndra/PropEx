<h1 class="sold">Sold Properties</h1>

<section class="property-grid grid">
  <div class="container">
    <div class="row">
      <% @sold_property.each do |property| %>
        <div class="box">
          <div class="row">
            <div class="col-sm-12 col-md-6">
              <div class="testimonial-img">
                <%= link_to property, class: "link-a" do  %>
                  <%= render property %>
                <% end %>
              </div>
            </div>
            <div class="col-sm-12 col-md-6">
              <%= render partial: "property_details", locals: {property: property} %>
              <div class="testimonial-author-box text-center">
                <h2>Buyer Details:</h2>
              </div>
              <div class="content">
                <p class="testimonial-text fs-5">
                  <strong>Name: </strong><%=  user(property.buyer_id).name %><br/>
                  <strong>Email: </strong><%=  user(property.buyer_id).email %>
                </p>
              </div>
            </div>
          </div>
        </div>            
      <% end %>
    </div>
  </div>
</section>

<!-- ======= Footer ======= -->
<%= render 'layouts/section_footer' %>